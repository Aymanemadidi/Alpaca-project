{eyeArr.map((acc, index) => {
            Object.keys(state).forEach((stateItem, i) => {
              return (
                <img
                  className={`${stateItem ? "block" : "hidden"}`}
                  src={acc.item}
                  alt="eye"
                  key={acc.id}
                />
              );
            });
          })}